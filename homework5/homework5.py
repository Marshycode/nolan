# Git is a coding service local to your computer and GitHub is a web based version that holds git repositories
# Terminal is a place to textually interact with your computer and cmmd line is a specific line in the terminal. 
# Local repository is on your computer only and a remote repository is linked to the cloud. 
# version control track changes in an og code. 
# staging area is where changs are saved localluy before going to the cloud, last spot to make edits. 
#git add adds somthing to the stagin ares
# git commit commits somthing after in stagin area
#  git push sends the commited items to the cloud
# git status shows you your working tree
# git pull takes a repository on the cloud and brings it to your local computer
# pwd stands for print working directory and prints your current file
# ls lists everything in a file
# cd changes directories/files
# nano is a built in text editor
# touch creaes a file
# mv moves a file to a specified folder
# rm removes or deltes a file/folder
# cat concaticats or joins multiple files together
# 1: You have been plopped into Judy’s directory system. What command will tell you what your current working directory is?
# pwd
# 2: ls will list files
# 3:return to the python decal folder then use git pull brianna_repo
# 4:use the mv or cp command and then place it in yourname repository. 
# 5: cd ~ then move down to yornmane repository
# 6: nano homework.py would allow you to see and edit anythin in the python file. 
# 7: git add . #git commit -m "done with hw" #git push origin main
# 8: The remote has work you do not, first git pull origin main to yourname repository then go through steps in seven again
# 9: ~/recents/
print(type( "hello"))
def evn_or_odd(input):
    x=input
    if (input%2)==0:
        print("Even")
    else:
        print("Odd")
input=12
evn_or_odd(input)
numbers=numbers = [1, 2, 3, 4, 5]
def sumWithLoop(numbers):
    x=0
    for number in numbers:
        x+=number 
    return x
print(sumWithLoop(numbers))
my_list=["a", "b", "c"]
def duplicateList(my_list):
    x=[]
    for item in my_list:
        x+=[item,item]
    print(x)
duplicateList(my_list)
def square(num):
    return num * num
num=3
print(square(num))

